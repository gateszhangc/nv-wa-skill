apiVersion: v1
kind: Service
metadata:
  name: nv-wa-skill
  labels:
    app.kubernetes.io/name: nv-wa-skill
    app.kubernetes.io/part-of: nv-wa-skill
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nv-wa-skill
    app.kubernetes.io/part-of: nv-wa-skill
  ports:
    - name: http
      port: 80
      targetPort: 3000
